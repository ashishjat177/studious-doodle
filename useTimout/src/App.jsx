import './App.css'

// create a hook to easily use setTimeout (useCallback, delay)

// Reset the timer if delay changes

// do not reset the timer if only callback changes

function App() {

  return (
    <>
      
    </>
  )
}

export default App
